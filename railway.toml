[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Volume configuration for persistent data storage
# This ensures gm_data.json persists across deployments
[[deploy.volumes]]
mountPath = "/data"
name = "gm_bot_data"
